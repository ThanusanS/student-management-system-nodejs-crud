# Student Management System

Minimal project skeleton with backend (Express) and frontend (static HTML/JS).

Quick start:

1. Install dependencies:

```bash
npm install
```

2. Start server (development):

```bash
npm run dev
```

3. Open http://localhost:5000 in your browser.

Note: The backend controller uses an in-memory array for student data in this skeleton.
